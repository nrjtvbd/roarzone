name: Update Roarzone Playlist

on:
  schedule:
    - cron: '*/30 * * * *' # প্রতি ৩০ মিনিট পর পর চলবে
  workflow_dispatch: # ম্যানুয়ালি রান করার জন্য

permissions:
  contents: write # এটি না থাকলে গিটহাব ফাইল সেভ করতে পারবে না

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install requests

      - name: Run Script
        run: python update_roarzone.py

      - name: Commit and Push
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add playlist.m3u
          git commit -m "Auto-update token: $(date)" || echo "No changes to commit"
          git push
